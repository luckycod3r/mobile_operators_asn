# 📡 Mobile-Only VLESS Access (RU Operators)

Репозиторий для ограничения доступа к **VLESS/Xray** только через мобильные сети операторов РФ:

✅ МТС  
✅ Билайн  
✅ Мегафон  
✅ T2 (Tele2)

Идея простая: сервер принимает подключения к VLESS порту **только с IP-префиксов операторов**, все остальные (Wi-Fi/домашние провайдеры/офисные сети) блокируются через `ipset + iptables`.

---

## ⭐ Как это работает

1) В репозитории хранятся списки **ASN операторов** (`*.asn.txt`)
2) Скрипт генерирует из них **IP-префиксы** (`*.ip.txt`) через RIPEstat
3) Скрипт применяет `iptables` правила:
   - ✅ `ACCEPT` на VLESS порт только из `ipset mobile4`
   - ❌ `DROP` для всех остальных

---

## 📁 Структура репозитория

### ASN списки (источник)
- `mts.asn.txt`
- `beeline.asn.txt`
- `megafon.asn.txt`
- `t2.asn.txt`

### IP-префиксы (генерируются автоматически)
- `mts.ip.txt`
- `beeline.ip.txt`
- `megafon.ip.txt`
- `t2.ip.txt`